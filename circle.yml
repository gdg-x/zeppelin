machine:
  ruby:
    version: 2.5.0
  environment:
    NOKOGIRI_USE_SYSTEM_LIBRARIES: true # speeds up installation of html-proofer

dependencies:
  post:
    - git config --global user.email "rockerhieu+circleci@gmail.com"
    - git config --global user.name "Circle CI"
#    - gem install jgd

deployment:
  prod:
    branch: master
    commands:
      - bundle exec jgd -b master -u git@github.com:droidconvn/droidconvn.github.io.git